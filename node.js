class Node {
    constructor(x, y, depth, parent) {
        //x, y, depth and parent of each node -- pretty self explanatory
        this.x = x;
        this.y = y;
        this.depth = depth;
        this.parent = parent;
    }
}